// Global variables – can be used in ANY function below 
int x = 0;

// Runs once
void setup() {

  // Create canvas
  size(800, 500);
  
}

// Runs repeatedly
void draw() {
  
  // Draw the ellipse on screen
  ellipse(x, 50, 100, 100);
  
}
